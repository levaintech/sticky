[build.environment]
NPM_FLAGS="--version" # prevent Netlify npm install

[build]
publish = "packages/sticky-docs/.contented/.preview/out/"
command = "npm install pnpm && pnpm i --frozen-lockfile && turbo run generate --filter=sticky-docs --force"